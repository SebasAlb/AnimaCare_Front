import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Paleta original (comentada para futura referencia)
  static const Color oldBackground = Color(0xFFA6DCEF);
  static const Color oldHeader = Color(0xFF3E0B53);
  static const Color oldSecondary = Color(0xFF75C9C8);
  static const Color oldHighlight = Color(0xFFFFA726);

  // 🎨 Nueva Paleta actual
  static const Color background =
      Color(0xFF4DD0E2); // Fondo general (Celeste claro)
  static const Color header = Color(0xFF301B92); // Header (Turquesa oscuro)
  static const Color cardBackground = Colors.white; // Fondo de tarjetas
  static const Color labelBackground =
      Color(0xFF35919E); // Fondo de label eventos
  static const Color primaryWhite = Colors.white; // Blanco principal

  // 🎨 Colores de eventos
  static const Color eventBath =
      Color(0xFF4FC3F7); // Eventos de baño (Azul claro)
  static const Color eventVetConsult =
      Color(0xFF81C784); // Consultas veterinarias (Verde pastel)
  static const Color eventMedicine =
      Color(0xFFFFA726); // Medicinas (Amarillo pastel)
  static const Color eventVaccine = Color(0xFF64B5F6); // Vacunas (Celeste)
  static const Color eventOther =
      Color(0xFFBA68C8); // Otros eventos (Púrpura claro)

  // 🎨 Colores de resaltado
  static const Color markerBorder =
      Color(0xFFFFD54F); // Resaltado de días cargados (Amarillo dorado pastel)
  static const Color selectedDay =
      Color(0xFF301B92); // Día seleccionado (Azul oscuro medio púrpura)

  // 🎨 Botones y acciones
  static const Color formatButtonBackground = Colors.white;
  static const Color formatButtonForeground =
      Color(0xFF35919E); // Color del texto de botones de formato

  // 🎨 Otros
  static const Color transparent = Colors.transparent;
}
